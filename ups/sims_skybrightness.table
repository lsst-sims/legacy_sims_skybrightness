setupRequired(base)
setupRequired(ephem)
setupRequired(sims_utils)
setupRequired(sims_photUtils)
setupRequired(utils)
setupRequired(throughputs)
setupRequired(sims_skybrightness_data)
envPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)
envSet(SIMS_SKYBRIGHTNESS_DIR, ${PRODUCT_DIR})
